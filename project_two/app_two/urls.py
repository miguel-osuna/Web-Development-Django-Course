from django.urls import path
from app_two import views


urlpatterns = [
    #path("", views.help, name="help"),
    #path("", views.users, name="users"),
    #path("formpage", views.form_name, name="form name")
]
